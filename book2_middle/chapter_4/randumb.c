#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // long int hat;
    int hat;
    // hat = random();
    hat = rand();
    // printf("%ld is random number\n", hat);
    printf("%d is random number\n", hat);
    return 0;
}